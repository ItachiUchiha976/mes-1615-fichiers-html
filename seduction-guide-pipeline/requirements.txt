# Google Drive access
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.1.0

# Video download / audio extraction
yt-dlp>=2024.3.10
ffmpeg-python>=0.2.0

# Transcription locale – faster-whisper (recommandé : 4x plus rapide, moins de RAM)
# Utilise le modèle large-v3 en int8 quantisé → tourne bien sur CPU
faster-whisper>=1.0.0

# Fallback : openai-whisper classique (si faster-whisper non disponible)
openai-whisper>=20231117

# Client OpenAI (pour Whisper API cloud, optionnel)
openai>=1.14.0

# Gemini API
google-generativeai>=0.5.0

# Claude / Anthropic API (recommandé pour la génération du guide)
anthropic>=0.25.0

# Utilities
tqdm>=4.66.0
tiktoken>=0.6.0
python-dotenv>=1.0.0
